spring:
  datasource:
    url: jdbc:mysql://mysql:3306/distributed_system?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root123
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql
  data:
    redis:
      host: redis
      port: 6379
      timeout: 2000ms
  h2:
    console:
      enabled: false

server:
  port: ${SERVER_PORT:8080}

app:
  instance-id: ${INSTANCE_ID:local}
